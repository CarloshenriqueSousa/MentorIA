spring:
  application:
    name: mentoria-backend

  datasource:
    url: jdbc:postgresql://db.aegjansdipbegfgtqznu.supabase.co:5432/postgres?user=postgres&password=[YpddUV5HQ$vv2M03&5hvyZqFB8*6A^kYp00txtDmtkFGE9nj3KkTDqXQ!F5Y]
    username: postgres
    password: YpddUV5HQ$vv2M03&5hvyZqFB8*6A^kYp00txtDmtkFGE9nj3KkTDqXQ!F5Y
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: UTC

server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  secret: ${JWT_SECRET:MentorIASecretKeyMustBeAtLeast256BitsLongForSecurity}
  expiration: 86400000
  refresh-expiration: 604800000

ai-service:
  url: ${AI_SERVICE_URL:http://localhost:8000}

stripe:
  secret-key: ${STRIPE_SECRET_KEY:sk_test_placeholder}
  webhook-secret: ${STRIPE_WEBHOOK_SECRET:whsec_placeholder}
  price-basic: ${STRIPE_PRICE_BASIC:price_placeholder}
  price-premium: ${STRIPE_PRICE_PREMIUM:price_placeholder}

freemium:
  daily-message-limit: 10
  monthly-plan-limit: 2

cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:3000}

management:
  endpoints:
    web:
      exposure:
        include: health,info